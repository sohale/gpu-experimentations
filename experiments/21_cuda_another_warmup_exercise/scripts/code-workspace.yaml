# You need to run this to take effect:
#     ./scripts/dev.bash
#     Then, click on exper21.generated.code-workspace annd click ...

# see https://code.visualstudio.com/docs/reference/variables-reference
#     ${userHome}
#     ${env:USER}
#     "${workspaceFolder}/...


folders:
  - path: /home/paperspace/gpu-experimentations/experiments/21_cuda_another_warmup_exercise
   #      fails: ${userHome}/gpu-experimentations/experiments/21_cuda_another_warmup_exercise

settings:

  terminal.integrated.profiles.linux:

    expermatazoid21:
      path: /bin/bash
      args:
        - -c
        - "export TEST_ENV1=V1 && cd '${workspaceFolder}/' && echo 'Hello user: \"${env:USER}\":' ; exec bash"

  terminal.integrated.defaultProfile.linux: expermatazoid21
  terminal.integrated.cwd: ${workspaceFolder}

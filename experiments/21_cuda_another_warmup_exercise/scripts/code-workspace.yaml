# see https://code.visualstudio.com/docs/reference/variables-reference
#     ${userHome}
#     ${env:USERNAME}
#     "${workspaceFolder}/...


folders:
  - path: ${userHome}/gpu-experimentations/experiments/21_cuda_another_warmup_exercise

settings:

  terminal.integrated.profiles.linux:

    expermatazoid21:
      path: /bin/bash
      args:
        - -c
        - "export TEST_ENV1=V1 && 'cd ${workspaceFolder}/ && echo 'Hello ${env:USERNAME} ok'"

  terminal.integrated.defaultProfile.linux: expermatazoid21
  terminal.integrated.cwd: ${workspaceFolder}
